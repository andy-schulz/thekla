export interface TheklaAssertionOptions {
    // deep assertion like deep equal shall be used
    deep: boolean;

    // if expression shall be negated the attribute is set to true
    // default: false
    not: boolean;
}